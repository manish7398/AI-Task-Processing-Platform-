apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: mern-ai-tasks

resources:
  - namespace.yaml
  - configmap.yaml
  - secrets.yaml
  - redis.yaml
  - backend.yaml
  - frontend.yaml
  - worker.yaml
  - ingress.yaml

commonLabels:
  app: mern-ai-tasks
  environment: production